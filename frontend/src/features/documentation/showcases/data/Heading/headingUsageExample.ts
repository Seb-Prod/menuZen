const headingUsageExample = `import Heading from "@/components/ui/Heading";

export default function MyComponent() {
  return (
    <Heading variant={1} color="primary">
        Titre principal
    </Heading>
  );
}`;

export default headingUsageExample;